enum ChatMessageType {
  none,
  image,
  audio,
  document,
  video,
  text,
}

enum NoteCardType {
  notebook,
  section,
  notes,
}

enum SearchBarState {
  stattic,
  dynamic,
}

enum SearchedTileType {
  page,
  section,
}
